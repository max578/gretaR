% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_vi.R
\name{variational}
\alias{variational}
\title{Variational Inference (ADVI)}
\usage{
variational(
  model,
  n_samples = 1L,
  max_iter = 5000L,
  learning_rate = 0.01,
  tolerance = 1e-04,
  method = c("meanfield", "fullrank"),
  init_from_map = TRUE,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{model}{A \code{gretaR_model} object created by \code{\link[=model]{model()}}.}

\item{n_samples}{Number of Monte Carlo samples per ELBO gradient estimate
(default 1).}

\item{max_iter}{Maximum number of optimisation iterations (default 5000).}

\item{learning_rate}{Adam learning rate (default 0.01).}

\item{tolerance}{Convergence tolerance on relative ELBO change (default 1e-4).}

\item{method}{Variational family: \code{"meanfield"} (default) or \code{"fullrank"}.}

\item{init_from_map}{Logical; initialise from MAP estimate (default TRUE).}

\item{seed}{Optional integer seed for reproducibility. Sets both R and torch
random number generators.}

\item{verbose}{Logical; print progress (default TRUE).}
}
\value{
A \code{gretaR_fit} object (method = "vi") with standard fields plus:
\describe{
\item{par}{Named vector of posterior means (constrained space).}
\item{sd}{Named vector of posterior SDs (unconstrained space).}
\item{covariance}{Posterior covariance matrix (unconstrained, fullrank only).}
\item{elbo}{Vector of ELBO values per iteration.}
\item{draws}{A \code{posterior::draws_array} of samples from the variational posterior.}
\item{converged}{Logical; did the optimiser converge?}
}
}
\description{
Fit a model using Automatic Differentiation Variational
Inference. Approximates the posterior with a multivariate Gaussian
in unconstrained parameter space.
}
\examples{
\dontrun{
mu <- normal(0, 10)
sigma <- half_cauchy(2)
y <- as_data(rnorm(50, 3, 1.5))
distribution(y) <- normal(mu, sigma)
m <- model(mu, sigma)
fit <- variational(m)
coef(fit)
}
}
