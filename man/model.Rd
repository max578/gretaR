% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model}
\alias{model}
\title{Create a gretaR Model}
\usage{
model(..., precision = c("float32", "float64"))
}
\arguments{
\item{...}{\code{gretaR_array} objects representing the parameters of interest.}

\item{precision}{Torch dtype: \code{"float32"} (default) or \code{"float64"}.}
}
\value{
A \code{gretaR_model} object with \code{log_prob()} and
\code{grad_log_prob()} methods.
}
\description{
Compile the computation graph defined by the specified target
variables into a differentiable log-joint-density function suitable for
HMC/NUTS inference.
}
\examples{
\dontrun{
alpha <- normal(0, 10)
beta <- normal(0, 5)
sigma <- half_cauchy(1)
y <- as_data(rnorm(100))
x <- as_data(rnorm(100))
mu <- alpha + beta * x
distribution(y) <- normal(mu, sigma)
m <- model(alpha, beta, sigma)
}
}
