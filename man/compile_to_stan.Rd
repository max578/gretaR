% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_backend.R
\name{compile_to_stan}
\alias{compile_to_stan}
\title{Generate Stan Code from a gretaR Model}
\usage{
compile_to_stan(model)
}
\arguments{
\item{model}{A \code{gretaR_model} object created by \code{\link[=model]{model()}}.}
}
\value{
A character string containing valid Stan code.
}
\description{
Translate a compiled \code{gretaR_model} into Stan code suitable
for use with \code{cmdstanr}. The generated code preserves the model structure
defined via the gretaR DSL.
}
\examples{
\dontrun{
mu <- normal(0, 10)
sigma <- half_cauchy(2)
y <- as_data(rnorm(50, 3, 1.5))
distribution(y) <- normal(mu, sigma)
m <- model(mu, sigma)
cat(compile_to_stan(m))
}
}
