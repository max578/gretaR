% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{gretaR_abort}
\alias{gretaR_abort}
\title{Raise a classed gretaR refusal}
\usage{
gretaR_abort(
  message,
  reason_code,
  ...,
  call = rlang::caller_env(),
  .envir = parent.frame()
)
}
\arguments{
\item{message}{A cli-formatted message (character vector or bullet list),
passed straight to \code{\link[cli:cli_abort]{cli::cli_abort()}}.}

\item{reason_code}{One of \code{.gretaR_reason_codes}, identifying \emph{why} the
call was declined: \code{"unsupported_distribution"} (a distribution or
operation gretaR does not implement), \code{"untransformable_constraint"} (a
parameter, index, or dimension that cannot be resolved or reconciled),
\code{"backend_unavailable"} (a required optional dependency, e.g.
\code{cmdstanr}/\code{bayesplot}/\code{Matrix}, is not installed), \code{"nonconvergence"}
(a diagnostic the caller asked for is not computable because sampling
did not produce it), or \code{"invalid_input"} (the call's own arguments
fail validation at the boundary).}

\item{...}{Further arguments forwarded to \code{\link[cli:cli_abort]{cli::cli_abort()}} (e.g.
\code{call}).}

\item{call}{The calling environment, forwarded to \code{\link[cli:cli_abort]{cli::cli_abort()}} so
the error still reports the user-facing call site.}

\item{.envir}{Environment for cli glue interpolation (\code{{arg}} markers in
\code{message}), forwarded to \code{\link[cli:cli_abort]{cli::cli_abort()}}. Defaults to the frame that
called \code{gretaR_abort()}, matching the resolution scope a direct
\code{cli::cli_abort()} call at that site would have had -- glue markers
referring to a caller-local variable (e.g. a \code{for}-loop's \code{arg}) resolve
there rather than inside this wrapper's own frame.}
}
\value{
Does not return; raises a condition of class
\code{c("gretaR_<reason_code>_refusal", "gretaR_refusal", "orchestra_refusal", "rlang_error", "error", "condition")}.
}
\description{
Thin wrapper around \code{\link[cli:cli_abort]{cli::cli_abort()}} that stamps every gretaR error with
a class vector an orchestra leader can route on: a reason-specific class
(\verb{gretaR_<reason_code>_refusal}), the package-wide class (\code{gretaR_refusal}),
and the federation-shared marker (\code{orchestra_refusal}), ahead of the
\code{rlang_error}/\code{error}/\code{condition} classes \code{cli::cli_abort()} already
supplies. Every \code{cli_abort()} call site in \verb{R/} that represents gretaR
declining to proceed calls this instead.
}
\keyword{internal}
