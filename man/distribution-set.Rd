% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array.R
\name{distribution<-}
\alias{distribution<-}
\title{Assign a Distribution (Likelihood) to Observed Data}
\usage{
distribution(x) <- value
}
\arguments{
\item{x}{A data \code{gretaR_array} (created with \code{\link{as_data}}).}

\item{value}{A distribution \code{gretaR_array} (e.g., from \code{\link{normal}}).}
}
\value{
The data \code{gretaR_array} \code{x}, invisibly.
}
\description{
Define the likelihood by assigning a distribution to a data
\code{gretaR_array}. This registers the distribution as a likelihood
term in the model's log-joint density.
}
\examples{
\dontrun{
y <- as_data(rnorm(100))
mu <- normal(0, 10)
sigma <- half_cauchy(1)
distribution(y) <- normal(mu, sigma)
}
}
