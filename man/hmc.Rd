% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{hmc}
\alias{hmc}
\title{Run HMC Sampling}
\usage{
hmc(model, n_samples = 1000L, warmup = 1000L, chains = 4L, ...)
}
\arguments{
\item{model}{A \code{gretaR_model} object created by \code{\link{model}}.}

\item{n_samples}{Number of post-warmup samples per chain (default 1000).}

\item{warmup}{Number of warmup (adaptation) iterations per chain (default 1000).}

\item{chains}{Number of independent chains (default 4).}

\item{...}{Additional arguments passed to \code{\link{mcmc}}.}
}
\value{
A \code{gretaR_draws} object.
}
\description{
Convenience wrapper around \code{\link{mcmc}} that selects the
static Hamiltonian Monte Carlo sampler.
}
\examples{
\dontrun{
m <- model(normal(0, 1))
draws <- hmc(m, n_samples = 500, warmup = 500)
}
}
