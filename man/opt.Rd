% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimise.R
\name{opt}
\alias{opt}
\title{Find the Maximum A Posteriori (MAP) Estimate}
\usage{
opt(
  model,
  max_iter = 2000L,
  learning_rate = 0.01,
  tolerance = 1e-06,
  init = NULL,
  verbose = TRUE,
  backend = c("torch", "stan"),
  seed = NULL
)
}
\arguments{
\item{model}{A \code{gretaR_model} object created by \code{\link[=model]{model()}}.}

\item{max_iter}{Maximum number of optimisation iterations (default 2000).}

\item{learning_rate}{Adam learning rate (default 0.01).}

\item{tolerance}{Convergence tolerance on relative change in log-prob
(default 1e-6).}

\item{init}{Optional initial values (numeric vector in unconstrained space).}

\item{verbose}{Logical; print progress (default TRUE).}

\item{backend}{Inference backend: \code{"torch"} (default) or \code{"stan"}.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \code{gretaR_fit} object (method = "map") with components:
\describe{
\item{par}{Named numeric vector of MAP estimates (constrained space).}
\item{par_unconstrained}{Numeric vector of MAP in unconstrained space.}
\item{log_prob}{Log-joint density at the MAP.}
\item{convergence}{List with convergence info.}
\item{iterations}{Number of iterations used.}
}
}
\description{
Optimise the log-joint density using gradient-based methods
to find the posterior mode. Uses the Adam optimiser via torch.
}
\examples{
\dontrun{
mu <- normal(0, 10)
sigma <- half_cauchy(2)
y <- as_data(rnorm(50, 3, 1.5))
distribution(y) <- normal(mu, sigma)
m <- model(mu, sigma)
fit <- opt(m)
fit$par
}
}
