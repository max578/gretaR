% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{beta_dist}
\alias{beta_dist}
\title{Beta Distribution}
\usage{
beta_dist(alpha, beta, dim = NULL, truncation = NULL)
}
\arguments{
\item{alpha}{First shape parameter (positive numeric or \code{gretaR_array}).}

\item{beta}{Second shape parameter (positive numeric or \code{gretaR_array}).}

\item{dim}{Integer vector of dimensions (default scalar).}

\item{truncation}{Optional length-2 numeric vector \code{c(lower, upper)}
specifying truncation bounds. Default \code{NULL} (no truncation).}
}
\value{
A \code{gretaR_array}.
}
\description{
Create a beta-distributed variable with the specified shape
parameters. Support is on the interval \code{(0, 1)}. Named
\code{beta_dist} to avoid conflict with \code{base::beta}.
}
\examples{
\dontrun{
p <- beta_dist(alpha = 2, beta = 5)
}
}
