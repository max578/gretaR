% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom.R
\name{custom_distribution}
\alias{custom_distribution}
\title{Custom Distribution}
\usage{
custom_distribution(
  log_prob_fn,
  constraint = NULL,
  dim = NULL,
  name = "custom"
)
}
\arguments{
\item{log_prob_fn}{A function \verb{f(x) -> scalar torch tensor} computing
the log-probability density at \code{x}.}

\item{constraint}{Optional list with \code{lower} and \code{upper} bounds for the
parameter space. Determines the transform for HMC sampling.}

\item{dim}{Dimensions of the variable.}

\item{name}{Optional name for display purposes.}
}
\value{
A \code{gretaR_array} representing a variable with the custom distribution.
}
\description{
Define a distribution with a user-supplied log-probability
density function. The function must accept a torch tensor and return a
scalar torch tensor. It must be differentiable via torch autograd.
}
\examples{
\dontrun{
# Laplace distribution (not built in)
x <- custom_distribution(
  log_prob_fn = function(x) -torch_sum(torch_abs(x)),
  name = "laplace"
)

# Truncated normal (positive only)
x <- custom_distribution(
  log_prob_fn = function(x) {
    torch_sum(-0.5 * x^2)  # kernel of N(0,1)
  },
  constraint = list(lower = 0, upper = Inf),
  name = "truncated_normal"
)
}
}
