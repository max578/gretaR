% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array.R
\name{as_data}
\alias{as_data}
\title{Wrap Observed Data as a gretaR Array}
\usage{
as_data(x)
}
\arguments{
\item{x}{A numeric vector, matrix, or array of observed data.}
}
\value{
A \code{gretaR_array} representing fixed (observed) data.
}
\description{
Convert numeric vectors, matrices, or arrays into a
\code{gretaR_array} data node for use in model definitions. Data nodes
are fixed (not sampled) during inference.
}
\examples{
\dontrun{
y <- as_data(iris$Sepal.Length)
X <- as_data(matrix(rnorm(30), ncol = 3))
}
}
